# [模块名称] 设计文档

## 1. 概述 (Overview)
简述模块的功能、在流水线中的位置以及主要设计目标。

## 2. 接口定义 (Interface Definition)
使用表格列出模块的主要输入和输出信号。位宽应尽可能使用参数表示（如 `FETCH_WIDTH * 32`）。

| 信号名 | 位宽 (bits) | 方向 | 来源/去向 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `clk` | 1 | 输入 | 全局 | 时钟信号 |
| `reset` | 1 | 输入 | 全局 | 复位信号 |
| `...` | `PARAM * N` | ... | ... | ... |

## 3. 微架构设计 (Microarchitecture)

### 3.1 逻辑结构
使用 Mermaid 流程图或文字描述模块内部的逻辑分区。

### 3.2 核心工作流
描述模块在正常运行、分支预测失败、异常等场景下的工作逻辑。

## 4. 组合逻辑功能描述 (Combinational Logic)
针对代码中定义的各个 `comb_xxx` 函数，详细描述其逻辑功能及产生的关键信号。

### 4.1 `comb_function_name_1`
- **功能描述**：...
- **产生的关键信号**：...

### 4.2 `comb_function_name_2`
- **功能描述**：...
- **产生的关键信号**：...

## 5. 性能计数器 (Performance Counters)
描述该模块相关的性能统计指标（用于 TMA 分析或瓶颈定位）。

| 计数器名称 | 指标含义 | TMA 层级 | 描述 |
| :--- | :--- | :--- | :--- |
| `...` | ... | ... | ... |

## 6. 资源占用 (Resource Usage)

### 6.1 存储资源 (Storage Resources)
列出模块内部定义的持久状态元素（对应代码中的 `reg` 类型变量、SRAM 或流水线寄存器）。

| 寄存器/存储名称 | 规格 (Size/Bits) | 硬件类型 | 描述 |
| :--- | :--- | :--- | :--- |
| `...` | `WIDTH` | `reg` | ... |

### 6.2 硬件开销 (Hardware Overhead)
描述模块的计算逻辑、并发处理能力（如端口数、各阶段宽度）等。

| 资源名称 | 规格 (Ports/Width) | 描述 |
| :--- | :--- | :--- |
| `...` | `FETCH_WIDTH` | ... |
